# Camera originals — 369 MB, the source of truth, but never published and far
# too large for a git repo. Back these up separately.
originals/

node_modules/
dist/
entries.txt

# Build output. Regenerate with ./build-images.sh and npm run build.
images/
js/

.DS_Store
.wrangler/

# JetBrains
.idea/
*.iml

# Wrangler local secrets. Never commit these.
.dev.vars
.dev.vars.*

.env
.env.*

*.tsbuildinfo
npm-debug.log*

# Machine-local Claude Code permissions; a shared .claude/settings.json can be committed.
.claude/settings.local.json
